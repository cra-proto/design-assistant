{
    "name": "usage-lambda",
    "version": "1.0.0",
    "description": "Lambda function for usage tracking",
    "main": "index.js",
    "scripts": {
        "build": "tsc",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "@aws-sdk/client-dynamodb": "^3.0.0",
        "@aws-sdk/lib-dynamodb": "^3.0.0"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.0",
        "@types/node": "^22.19.1",
        "typescript": "^5.0.0"
    }
}